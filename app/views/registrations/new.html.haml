#registrations-new
  .col-sm-6.col-sm-push-6.left-content
    %h1
      = link_to "makepo", posts_path
    %p.ex
      このサイトは、ネットショッピングのポータルサイトです！
      - if notice
        %p
          .alert.alert-info= notice

  .col-sm-6.col-sm-pull-6.right-content
    = form_for @user, url: registrations_path, method: :post do |f|
      %h2
        makepo　会員登録
      - @user.errors.each do |attr, message|
        .alert.alert-danger= message
      .form-group
        = f.label :name, "IDを入力する", class: "control-label"
        = f.text_field :name, class: "form-control"
      .form-group
        = f.label :email,"メールアドレスを入力する", class: "control-label"
        = f.text_field :email, class: "form-control"
      .form-group
        = f.label :password, "パスワード: 6文字以上", class: "control-label"
        = f.password_field :password, class: "form-control"
      .form-group
        = f.label :password_confirmation, "確認用パスワードです。もう一度記入ください", class: "control-label"
        = f.password_field :password_confirmation, class: "form-control"
        = f.submit "登録する", class: "btn"
        = link_to "ログインする", new_sessions_path, class: "login pull-right"
        .clear
